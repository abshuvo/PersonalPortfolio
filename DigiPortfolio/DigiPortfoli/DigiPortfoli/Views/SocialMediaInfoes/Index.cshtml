@model IEnumerable<DigiPortfoli.Models.Entities.SocialMediaInfo>

@{
    Layout = "~/Views/Shared/_LayoutWithNavbar.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PersonalInfoId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SocialMediaName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SocialMediaURL)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PersonalInfoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SocialMediaName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SocialMediaURL)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
